import * as React from "react";
import Layout from "../components/layout";

const IndexPage = () => {
  return (
    <Layout>
      <main>Index</main>
    </Layout>
  );
};

export default IndexPage;
